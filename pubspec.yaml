name: nulldle
description: A Nulldle Wordle‑style game with clean architecture.
publish_to: 'none'

environment:
  sdk: '^3.6.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Added to support linting rules referenced in analysis_options.yaml
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/words.txt
    - assets/title.png
    - assets/english_dict.txt
